## 1. Reviewed Roots and Lockfile Contract

- [ ] 1.1 Define closed managed-extension root DTOs and reuse the prerequisite-owned exact `[build.stages.base.node].node_version` and `.npm_version` fields in `docker-constructor.toml`; verify missing or malformed required values, unknown fields, runtime catalogs, and managed-extension overrides are rejected before execution, npm version is never inferred from the image tag, and this change introduces no second expected-version source.
- [ ] 1.2 Verify the reviewed toolchain baseline already bootstrapped by `materialize-build-artifacts-on-host` is reused unchanged: run `node --version` and `npm --version` in the currently reviewed digest-pinned Node image, compare them with the existing exact fields, verify the inspected image digest equals the current reviewed digest, and never derive or rewrite either value from its tag.
- [ ] 1.3 Add checked-in lockfile-v3 root/closure validation and verify a matching root set with a complete reachable dependency graph, required resolved sources, and SRI metadata is accepted while transitive packages remain non-root closure members.
- [ ] 1.4 Add rejection tests for missing, extra, and version-mismatched TOML roots; dangling root and transitive dependency edges; absent reachable package entries; missing registry `resolved` or `integrity`; and transitive-as-root packages, with diagnostics distinguishing root drift from incomplete closure.
- [ ] 1.5 Replace managed-extension effective runtime selection with reviewed-root/lock selection and verify no managed extension URL, artifact catalog, host path, or override reaches a runtime DTO.
- [ ] 1.6 Document the edit-roots → `sync-lock` → review → rebuild workflow and verify documentation contracts cover the breaking removal of runtime overrides.
- [ ] 1.7 Remove managed-extension roots, selections, artifact URLs, integrity values, catalogs, and cache paths from the effective runtime inventory DTO/projection and serialization; verify ordinary image-provided tool inventory remains available while no managed-extension data can enter a runtime projection.

## 2. All-or-Nothing Lock Synchronization

- [ ] 2.1 Add top-level `sync-lock` command parsing and a registry of Constructor-managed lock owners with deterministic ordering; register the managed Pi extension `package-lock.json` as the initial owner and verify dispatch is independent of ordinary `check-deps` provider reconciliation.
- [ ] 2.2 Deterministically render a minimal temporary `package.json` from canonically sorted reviewed exact roots and verify no transitive, local, runtime-selection, or unrelated inventory field enters the resolver manifest.
- [ ] 2.3 Implement managed-owner preflight that checks the closed inventory schema, exact root declarations, reviewed Node/npm pins, and safe checked-in lockfile path without comparing current roots with the existing lockfile or validating that lock's closure. Then execute the owner resolver in the reviewed digest-pinned Node image, compare `node --version` only with reviewed `node_version`, compare `npm --version` only with reviewed `npm_version`, and only then permit npm resolution. Add separate tests proving existing root drift does not block resolver dispatch, a Node mismatch prevents every npm command/provider request/network, and an npm mismatch permits only `npm --version` and prevents `npm install`, provider requests, and resolver network.
- [ ] 2.4 Invoke exactly `npm install --package-lock-only --ignore-scripts --no-bin-links --no-audit --no-fund --registry=https://registry.npmjs.org/` and verify argv order/policy is fixed, no lifecycle script executes, and the checked-in lockfile is not mounted as writable resolver output.
- [ ] 2.5 Run npm with an isolated temporary user config, empty global config, and cleared npm-configuration environment; verify project, user, and host-global `.npmrc`, `NPM_CONFIG_REGISTRY`, `NPM_CONFIG_USERCONFIG`, scope-registry mappings, and analogous variables cannot change the public registry, and a custom registry for a scoped package is never selected. Detect only the presence of known ambient files or variable names without reading contents or values; verify a prominent ignored-configuration warning appears when any are present and is absent when none are present.
- [ ] 2.6 Project only the resolved credential-free proxy and enabled corporate CA policy into resolver execution; verify enabled and disabled paths, rejection of credentials only in fixed/projected registry, proxy, or CA inputs before container or network execution, redaction from vectors, logs, exceptions, diagnostics, and candidate data, and no credential scanning of ignored `.npmrc` or discarded npm environment values.
- [ ] 2.7 Add resolver-boundary tests proving redirect following remains opaque pinned-npm behavior, no diagnostic claims pre-follow target validation, foreign or non-HTTPS `resolved` URLs are rejected during post-generation candidate validation, and acceptance is not reported as proof that transient redirect traffic did not occur.
- [ ] 2.8 Isolate the generated manifest, temporary npm configuration, and candidate `package-lock.json` in private staging and verify no other staging file is publishable or accepted as reviewed or assembly input.
- [ ] 2.9 After resolver completion, validate only the staged candidate as lockfile v3 with exact correspondence to the current TOML roots, complete reachable closure, registry metadata, and SRI before publication; verify every reachable registry package uses a credential-free HTTPS `resolved` origin of `registry.npmjs.org`, foreign origins/userinfo/non-HTTPS URLs are rejected, and every candidate failure leaves the prior checked-in lock byte-for-byte unchanged even when that prior lock has root drift.
- [ ] 2.10 Add a complete-closure resolver test proving the exact initial registry argv, isolated npm configuration, and absence of ambient overrides, then verify every reachable registry package in the accepted candidate has an HTTPS `resolved` origin of `registry.npmjs.org`; the test SHALL make no claim that npm emitted no transient cross-origin redirect request.
- [ ] 2.11 Implement one exception, signal, and cancellation cleanup path and verify it removes temporary manifest, npm configuration, candidate, and staging files, terminates resolver execution, preserves the prior lock bytes, and does not delete separately owned persistent download caches.
- [ ] 2.12 Implement durable atomic lockfile replacement and verify complete success replaces only the checked-in lockfile while replacement failure preserves its exact prior bytes and leaves no authoritative candidate.
- [ ] 2.13 Enforce the ownership boundary: the managed lock owner resolves and generates a candidate under `sync-lock`, while `locked-npm-environment-assembly` consumes only an approved lock through fixed `npm ci` and receives the same reviewed `node_version` and `npm_version` only as verification inputs; verify the assembler cannot invoke a Constructor metadata provider, derive or own a second expected-version source, resolve updates, rewrite roots, or publish a synchronized lockfile.
- [ ] 2.14 Add text and JSON reporting that identifies every synchronized lock owner and requires manual review and rebuild after `sync-lock`; verify synchronization never builds an image or changes reviewed TOML roots.
- [ ] 2.15 Add ordinary `check-deps` tests for the dedicated managed-root version provider: verify unscoped and scoped endpoints use only `https://registry.npmjs.org/`, exact roots are compared with latest eligible stable versions, a newer version yields a root-version-only suggestion, and authority/network/metadata failure yields `unavailable` without custom-registry fallback. Add protected-transport tests proving the closed provider API receives and inspects no npm environment/configuration, ambient HTTP authentication, `.netrc`, or cookie jar; request headers exclude authorization and cookies; optional proxy/CA inputs use only separately validated credential-free policy; allowed origin is fixed to `https://registry.npmjs.org:443`; same-origin redirects are bounded; and cross-origin, HTTP, userinfo-bearing, malformed, unsupported, or excessive redirects are rejected before a second request and without successful cache publication. Prove the provider never invokes the artifact-backed npm metadata provider, fetches tarball metadata, resolves or reads closure packages, reads or corrects removed runtime URL/SRI fields, or mutates the lockfile.
- [ ] 2.16 Add ordered managed-owner tests proving `sync-lock` performs drift-tolerant owner preflight, creates private staging, starts the reviewed digest-pinned image, verifies Node, verifies npm, runs npm resolution, and validates candidate root correspondence and closure before transaction readiness. Add one success case where TOML roots changed and the existing lock retains old roots but a matching valid candidate becomes ready, and one failure case where an invalid candidate leaves every prior lock byte-for-byte unchanged. Prove the owner performs no ordinary provider reconciliation or Constructor metadata-transport call, permits no resolver network before both assertions succeed, configures exactly `https://registry.npmjs.org/`, and emits only its validated candidate.
- [ ] 2.17 Implement all-owner orchestration that completes every owner preflight, candidate generation, and candidate validation before publication; verify one failing owner causes zero checked-in lock writes and deterministic cleanup of all candidates.
- [ ] 2.18 Implement a durable publication journal with complete prior bytes, candidate identities, target paths, and commit phase; add future multi-owner test doubles proving interruption after any individual replacement recovers to the complete prior or complete candidate set and never accepts a partial final state.
- [ ] 2.19 Verify the initial single-owner configuration uses the same transaction protocol, reduces to durable atomic replacement of its one target, cleans journal/backups only after completion, and keeps persistent download caches outside transaction ownership.

## 3. Host Assembly and Image Delivery

- [ ] 3.1 Invoke `locked-npm-environment-assembly` for managed roots and the checked-in lock with the inventory's reviewed `node_version` and `npm_version` as verification inputs, and verify a complete host-validated closure/evidence result is required before Docker without introducing assembler-owned expected versions.
- [ ] 3.2 Extend the post-materialization named-context plan for the managed extension tree/evidence and verify BuildKit rejects missing, corrupt, identity-mismatched, or evidence-mismatched input.
- [ ] 3.3 Copy the validated closure into `/opt/pi-extensions` in an independent image layer and verify BuildKit performs no extension npm networking or installation.
- [ ] 3.4 Add cache-boundary tests proving a root/lock change invalidates the extension layer without invalidating unrelated Pi or OpenSpec layers.

## 4. Pi Settings Integration and Guard

- [ ] 4.1 Implement constructor-owned sidecar transaction state for `~/.pi/agent/settings.json` and `~/.pi/agent/docker-constructor-managed-extensions.json`, with distinct `managedEntries` root/path ownership records and `suppressedBaselines` complete user-entry/restoration records plus durable atomic replacement of each file; verify schema separation, non-duplicate user npm, git, and local entries plus relative order are preserved, and exact qualifying npm baselines are retained.
- [ ] 4.2 Add table-driven managed-root source tests proving unscoped `<name>` maps exactly to `/opt/pi-extensions/node_modules/<name>`, scoped `@<scope>/<name>` maps exactly to `/opt/pi-extensions/node_modules/@<scope>/<name>`, one string entry is emitted per reviewed root, and neither the common `node_modules` directory nor any transitive package is registered.
- [ ] 4.3 Implement idempotent managed-entry reconciliation and verify missing roots are added, exact entries are not duplicated or reordered, stale owned paths are replaced, removed roots delete only their owned entries/records, and a resembling local user path without matching `managedEntries` ownership is preserved.
- [ ] 4.4 Implement crash-safe registration, duplicate suppression, and restoration as one logical settings/sidecar transaction: preserve qualifying user npm baselines before removal, finalize or safely roll back interrupted transactions deterministically, protect settings replacement with a lock or immediate compare-and-swap revalidation and conflict/retry behavior, never overwrite newer active user entries, and restore baselines only after managed-root removal with no equivalent active entry; verify multiple duplicates, version-insensitive normalized npm-name equivalence, git/local non-suppression, malformed/stale sidecar rejection, concurrent settings-write races, and active-managed-versus-suppressed-baseline diagnostics.
- [ ] 4.5 Add Pi integration tests that reconcile unscoped and scoped roots, invoke real package discovery/loading, verify each root's declared extension executes, and verify its transitive imports resolve from the shared `/opt/pi-extensions/node_modules` closure; a settings-JSON assertion alone SHALL NOT satisfy this task.
- [ ] 4.6 Implement exact wrapper classification for documented forms: `list`; `install <npm|git|URL|absolute-path|relative-path>`; `remove npm:<package>`; bare `update`; `update --all`; `update --extensions`; `update --models`; `update --self`; `update --self --force`; `update npm:<package>`; and `update --extension npm:<package>`. Add rejection tests for agent/broad updates and allow tests for `list` and `update --models`; do not infer aliases or classify unlisted forms.
- [ ] 4.7 Generate an immutable read-only managed-root manifest and implement canonical `npm:<name>[@<version>]` and scoped `npm:@<scope>/<name>[@<version>]` identity comparison only in recognized npm source slots. Add table-driven managed/unmanaged install/remove/update tests with and without versions; verify `git:`, raw URL, absolute-path, and relative-path installs forward unchanged, while malformed, percent-encoded, ambiguous, aliased, or otherwise unlisted forms remain outside wrapper classification and are forwarded to Pi.
- [ ] 4.8 Add table-driven argv tests proving exact documented forms are classified and every unlisted command or argv shape is forwarded without mutation-like heuristics. Cover unknown verb plus managed npm source, unknown verb plus unmanaged npm source, inferred aliases, reordered flags, `--`, multiple operands, conflicting selectors, and unknown options as unlisted forwarding cases. Separately verify every documented reject form—Pi agent update, broad extension update, or recognized source operation on a managed root—does not invoke Pi and prints roots → `sync-lock` → review → rebuild guidance.
- [ ] 4.9 Verify every rejection gives root/`sync-lock`/review/rebuild guidance and wrapper classification reads only the build-time manifest, never reads or changes settings/sidecar state, and never owns duplicate reconciliation or diagnostics.
- [ ] 4.10 Verify `list`, `update --models`, recognized unmanaged source operations, non-npm installs, ordinary commands, and every unlisted invocation preserve original argv byte-for-byte at exec plus signals and exit status; transparency SHALL depend only on the exact documented classifier, not on heuristic proof that an unknown invocation is non-mutating.

## 5. Remove Runtime Extension Mechanisms

- [ ] 5.1 Remove managed-extension runtime artifact materialization, runtime projection generation, per-package mounts, and related launch-vector inputs; verify `run` has no managed-extension cache or mount side effects.
- [ ] 5.2 Remove launch-time managed-extension installer, network, verification, cleanup, and legacy installation paths; verify container startup uses only `/opt/pi-extensions` and performs no npm operation.
- [ ] 5.3 Reject retired managed-extension runtime override options with migration guidance and verify an image built from reviewed roots remains usable after runtime cache cleanup.
- [ ] 5.4 Remove obsolete managed-extension runtime DTOs, `runtime-artifacts/blobs` cache namespaces, unreachable runtime-catalog validation and warnings, tests, and installer/mount documentation; verify `validate` emits no unreachable-catalog-version warning for managed Pi extensions while unrelated runtime artifacts and local corporate-network behavior remain intact.
- [ ] 5.5 Replace managed-extension runtime verification with image-owned closure verification: verify every reviewed root at its canonical `/opt/pi-extensions/node_modules` path against build-time evidence and the managed-root manifest, while proving verification invokes no installer, reads no effective runtime projection or mounted-artifact metadata, and does not inspect managed packages in the mounted Pi home; retain effective-inventory verification for ordinary image-provided tools.

## 6. End-to-End Validation

- [ ] 6.1 Add acceptance tests for ordinary `check-deps` reconciling exact managed roots through the fixed-public-registry root-version provider, including unscoped/scoped success, root-only suggestion, and unavailable authority, without artifact-backed metadata reconciliation, closure reads/resolution, removed runtime npm metadata, or lock mutation; ordinary `validate` and `build` rejecting existing root-lock drift; owner-specific synchronization preflight accepting that drift; successful generation and all-or-nothing publication of a candidate matching newly edited roots; invalid candidate preservation of the complete prior lock set byte-for-byte; complete ordered synchronization under the fixed public-registry configuration/candidate-acceptance boundary with no ordinary provider reconciliation; incomplete or dangling candidate closure; missing, extra, or version-mismatched candidate roots; separate Node mismatch with no npm command/provider request/network and npm mismatch with only `npm --version` but no install/provider request/network; exact npm argv; isolated npm config; user/project/global `.npmrc` and registry-environment non-interference without content/value scanning; conditional ambient-config warning presence and absence; scoped custom-registry non-selection; foreign lockfile-origin rejection; opaque npm redirect behavior and truthful reporting that absence of transient cross-origin traffic is not guaranteed without an out-of-scope network sandbox/egress allowlist; lifecycle-script suppression; credential rejection only for projected inputs; credential-free proxy/CA projection; failure and cancellation cleanup; prior-lock byte preservation; successful atomic publication; persistent-cache preservation; assembler use of the same reviewed toolchain values without a second source; assembler refusal to generate or update a lockfile; assembler evidence failure; and BuildKit no-network/no-install behavior.
- [ ] 6.2 Add acceptance tests for exact scoped/unscoped managed local-path registration; rejection of common-directory and transitive registration; idempotent add/update/remove; separation of `managedEntries` from `suppressedBaselines`; preservation of resembling user-owned local paths; real Pi discovery/loading of every reconciled root and transitive import resolution from the image-owned closure; non-duplicate settings preservation; crashes before sidecar publication, after sidecar preparation, and after settings replacement; deterministic recovery; managed-root removal; manually restored or changed user entries; multiple duplicate npm entries; malformed/stale sidecar; baseline-versus-effective diagnostics; every documented wrapper class including agent/broad update rejection, managed scoped/unscoped source rejection, unmanaged npm and git/URL/path forwarding, `list`, and `update --models`, plus transparent forwarding of aliases, reordered flags, `--`, conflicting/unknown options, malformed sources, and every other unlisted argv form; wrapper settings/sidecar non-ownership; ordinary command transparency; no runtime managed-extension npm operation; and real Pi loading of managed roots/transitive imports from `/opt/pi-extensions/node_modules` alongside a writable custom npm package from `~/.pi/agent/npm` without cross-store package/module resolution or mutation.
- [ ] 6.3 Run focused extension, lock, assembler, Dockerfile, launch, settings, and wrapper suites and verify every managed extension is image-owned and override-free.
- [ ] 6.4 Run project typecheck, lint, full tests, build/contracts, and the configured green loop; record a fully passing report.
- [ ] 6.5 Run end-to-end contracts proving managed-extension data is absent from the effective runtime inventory, runtime verification uses only `/opt/pi-extensions` plus build-time evidence/manifest without installer or mounted metadata, ordinary `check-deps` checks managed root versions only through the fixed-public-registry root provider and never invokes the artifact-backed npm metadata provider, and top-level `sync-lock` performs no ordinary provider request, is the sole closure-resolution path after successful toolchain assertions, validates every registered owner before publication, and leaves only the complete prior or complete synchronized lock set.
